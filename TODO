* Make sending long messages( partition of a long message )
* write installer
* more tests
* Make processing signals received from the operating system
* improve init-script:
** launch smstasks after redis-server
** rewrite init-script wits start-stop-daemon
** write the init-scripts for a freebsd
* write documentation for methods
* Sending messages to multiple threads( sent via an asynchronous module )
* rewrite documentation and comments on english
* Divide smstasks.pl into modules
